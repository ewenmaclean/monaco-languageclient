{
  "private": true,
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.7",
    "@types/vscode": "1.65.0",
    "editorconfig": "^0.15.3",
    "monaco-editor-core": "^0.33.0",
    "npm": "^8.5.5",
    "shx": "^0.3.4",
    "typescript": "^4.6.2"
  },
  "resolutions": {
    "vscode-languageserver-types": "3.16.0"
  },
  "scripts": {
    "clean": "npm run clean --workspaces",
    "dev": "npm run dev --workspace packages/client",
    "lint": "echo 'No linting yet'",
    "build-client": "npm run build --workspace packages/client",
    "build-example-client": "npm run build --workspace packages/example-client",
    "build-all": "npm run build-client && npm run build-example-client",
    "build-local": "npm i && npm run build-client && npm link ./packages/client && npm run build-example-client"
  },
  "workspaces": [
    "packages/client",
    "packages/example-client",
    "packages/example-browser"
  ]
}
